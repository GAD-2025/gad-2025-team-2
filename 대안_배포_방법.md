# 터미널 문제 해결 및 대안 배포 방법

## 🔍 터미널 문제 해결

### 방법 1: Windows PowerShell 직접 열기

Cursor 터미널이 안 되면 **Windows PowerShell을 직접** 열어보세요:

1. **Windows 키** 누르기
2. **"PowerShell"** 입력
3. **"Windows PowerShell"** 클릭 (관리자 권한으로 실행 아님)
4. 검은색/파란색 창이 열림
5. 여기서 SSH 접속 시도:
   ```powershell
   ssh root@route.nois.club
   ```

---

### 방법 2: 명령 프롬프트(cmd) 사용

1. **Windows 키 + R** 누르기
2. **"cmd"** 입력하고 **Enter**
3. 검은색 창이 열림
4. SSH 접속 시도:
   ```cmd
   ssh root@route.nois.club
   ```

---

## 🚀 대안: GitHub Push 후 자동 배포 확인

SSH 접속이 안 되더라도, **GitHub에 push하면 자동 배포**가 설정되어 있을 수 있습니다.

### 확인 방법:

1. **GitHub에 코드가 push되어 있는지 확인:**
   ```powershell
   cd gad-2025-team-2-main
   git status
   git log --oneline -5
   ```

2. **배포 서버 확인:**
   - 브라우저에서: https://route.nois.club:3002/docs
   - `/api/posts` 엔드포인트가 있는지 확인
   - `/employer/stores/{user_id}` 엔드포인트가 있는지 확인

3. **자동 배포가 설정되어 있다면:**
   - GitHub에 push 후 5-10분 기다리기
   - 배포 서버가 자동으로 업데이트됨

---

## 📋 현재 상황 확인

먼저 GitHub에 코드가 push되어 있는지 확인해봅시다:

```powershell
cd gad-2025-team-2-main
git status
git log --oneline -5
```

이 명령어들을 실행해서 확인해볼까요?


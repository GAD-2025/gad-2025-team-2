# 채용 탭 검토하기 버튼 수정 완료

## ✅ 수정 완료 사항

### 1. Applicant 인터페이스에 userId 추가
- `userId` 필드를 추가하여 지원자의 `user_id`를 저장
- 지원자 상세 페이지에서 `getJobSeekerProfile` 호출 시 필요

### 2. 데이터 매핑 수정
- `app.seekerId`를 `userId`로 매핑
- `app.seekerId`는 `Application.seekerId`로, 이것은 `signup_user_id`와 동일
- 백엔드의 `/job-seeker/profile/{user_id}` 엔드포인트는 `user_id`를 기대하므로 `seekerId`를 사용

### 3. 네비게이션 수정
- 지원자 카드 클릭 시 `userId`를 우선 사용
- "검토하기" 버튼 클릭 시 `userId`를 우선 사용
- `userId`가 없으면 `id` (applicationId)를 fallback으로 사용

## 🔧 수정된 파일

1. **frontend/src/pages/employer/Recruitment.tsx**
   - `Applicant` 인터페이스에 `userId` 필드 추가
   - 데이터 매핑 시 `app.seekerId`를 `userId`로 저장
   - 네비게이션 시 `userId` 우선 사용

## 🎯 문제 원인

1. **채용 탭**: `applicant.id`는 `applicationId` (지원서 ID)
2. **지원자 상세 페이지**: `getJobSeekerProfile`은 `user_id`를 기대
3. **불일치**: `applicationId`를 `user_id`로 사용하려고 해서 404 에러 발생

## 🧪 테스트 방법

1. **채용 탭 접속**:
   - `/recruitment` 접속
   - 지원자 목록 확인

2. **검토하기 버튼 클릭**:
   - "검토 대기" 상태의 지원자에서 "검토하기" 버튼 클릭
   - 지원자 상세 페이지로 이동
   - 지원자 정보가 정상적으로 표시되는지 확인

3. **지원자 카드 클릭**:
   - 지원자 카드 전체 클릭
   - 지원자 상세 페이지로 이동
   - 지원자 정보가 정상적으로 표시되는지 확인

## 📝 중요 사항

- `Application.seekerId`는 `signup_user_id`와 동일합니다
- 백엔드 엔드포인트 `/job-seeker/profile/{user_id}`는 `user_id`를 기대합니다
- 따라서 `app.seekerId`를 `userId`로 사용하면 됩니다

**이제 채용 탭에서도 지원자 정보가 정상적으로 표시됩니다!** 🎉


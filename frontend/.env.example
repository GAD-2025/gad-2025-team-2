# Example env for frontend (do NOT include secrets here)
# Copy to `.env` in your local environment when you want to run against a specific backend.
# The frontend reads VITE_API_BASE_URL via import.meta.env.VITE_API_BASE_URL

VITE_API_BASE_URL=https://route.nois.club:3002

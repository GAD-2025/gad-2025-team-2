# 배포 서버에 코드 반영 필요

## 🔍 문제

`/employer/stores/{user_id}` 엔드포인트가 배포 서버에 없음
- GitHub에는 코드가 있음
- 배포 서버에는 아직 반영되지 않음

---

## ✅ 확인 사항

### 1. GitHub에 코드가 있는지 확인

**employer.py에 stores 엔드포인트:**
- ✅ GitHub에 있음
- ✅ `get_stores` 함수 있음

**main.py에 employer router 포함:**
- ✅ GitHub에 있음
- ✅ `app.include_router(employer.router)` 있음

---

## 🚀 해결 방법

### 방법 1: 자동 배포 대기

**자동 배포가 설정되어 있다면:**
- GitHub push 후 5-10분 대기
- 배포 서버가 자동으로 코드를 가져와서 재시작

**확인 방법:**
- 5-10분 후 https://route.nois.club:3002/docs 다시 확인
- `/employer/stores/{user_id}` 엔드포인트가 나타나는지 확인

---

### 방법 2: 수동 배포 요청

**자동 배포가 안 되거나 시간이 오래 걸리는 경우:**

1. **서버 관리자에게 요청:**
   - "배포 서버에 최신 코드 반영 요청"
   - "GitHub의 main 브랜치에서 pull 후 서버 재시작 요청"

2. **또는 SSH 접속하여 직접 배포:**
   - SSH 접속 정보 확인
   - 서버에 접속하여 `git pull` 실행
   - 서버 재시작

---

### 방법 3: DATABASE_URL 설정으로 우회 (임시)

**배포 서버에 코드가 반영되기 전까지:**

1. **로컬 백엔드 실행:**
   ```powershell
   cd backend
   venv\Scripts\activate
   uvicorn app.main:app --reload
   ```

2. **로컬 백엔드가 배포 서버의 데이터베이스에 연결:**
   - `.env` 파일의 `DATABASE_URL` 설정 사용
   - 배포 서버의 데이터베이스에 직접 연결

3. **프론트엔드에서 로컬 백엔드 사용:**
   - 프론트엔드 `.env` 파일 수정:
     ```
     VITE_API_BASE_URL=http://localhost:8000
     ```
   - 로컬 백엔드 실행 중일 때만 작동

---

## 📋 체크리스트

### 배포 서버 반영 확인
- [ ] 5-10분 대기 후 API 문서 다시 확인
- [ ] `/employer/stores/{user_id}` 엔드포인트가 나타나는지 확인
- [ ] 엔드포인트가 정상 작동하는지 확인

### 대안 방법
- [ ] 서버 관리자에게 배포 요청
- [ ] 또는 로컬 백엔드 실행하여 임시 사용

---

## 🎯 권장 사항

1. **먼저 자동 배포 대기:**
   - 5-10분 후 https://route.nois.club:3002/docs 다시 확인

2. **여전히 없으면:**
   - 서버 관리자에게 배포 요청
   - 또는 로컬 백엔드 실행하여 임시 사용

---

## ⚠️ 주의사항

- 배포 서버에 코드가 반영되려면 시간이 걸릴 수 있음
- 자동 배포가 설정되어 있지 않으면 수동 배포 필요
- 로컬 백엔드는 임시 해결책일 뿐, 배포 서버에 반영하는 것이 최종 목표





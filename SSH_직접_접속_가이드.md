# SSH 직접 접속 가이드

## 🔍 현재 상황 확인

터미널에 메시지가 안 보인다면:

1. **다른 터미널 창**에서 SSH 접속을 시도했을 수 있습니다
2. **이미 접속이 완료**되어 있을 수 있습니다
3. **터미널이 다른 명령어 실행 중**일 수 있습니다

---

## ✅ 해결 방법

### 방법 1: 새 터미널에서 직접 접속

1. **새 터미널 창 열기**
   - Cursor에서: `Ctrl + Shift + `` (백틱)
   - 또는 하단 터미널 탭에서 "+" 버튼 클릭

2. **SSH 접속 명령어 입력**
   ```powershell
   ssh root@route.nois.club
   ```

3. **Enter 누르기**

4. **메시지가 나오면:**
   ```
   Are you sure you want to continue connecting (yes/no/[fingerprint])?
   ```
   → `yes` 입력하고 Enter

5. **비밀번호 입력**
   ```
   root@route.nois.club's password:
   ```
   → 비밀번호 입력 (화면에 표시 안 됨) 후 Enter

---

### 방법 2: 현재 터미널 상태 확인

현재 터미널에 무엇이 보이는지 확인:

- **프롬프트가 `PS C:\...>` 형태** → PowerShell 대기 중
  → `ssh root@route.nois.club` 입력

- **프롬프트가 `root@route:~#` 형태** → 이미 접속됨!
  → 바로 배포 명령어 실행 가능

- **다른 메시지가 보임** → 현재 실행 중인 작업 확인

---

### 방법 3: 자동으로 yes 입력하기

메시지가 안 나오는 경우, 자동으로 yes를 입력하는 방법:

```powershell
echo yes | ssh root@route.nois.club
```

또는:

```powershell
ssh -o StrictHostKeyChecking=no root@route.nois.club
```

---

## 📋 접속 후 확인

접속이 성공하면 프롬프트가 이렇게 바뀝니다:

```
root@route:~#
```

또는

```
[root@route ~]#
```

이 상태가 되면 배포 명령어를 실행할 수 있습니다!

---

## 🚀 배포 명령어 (접속 후)

```bash
# 프로젝트 디렉토리 찾기
find / -name "main.py" -path "*/app/main.py" 2>/dev/null | head -1

# 찾은 경로로 이동
cd /path/to/backend

# 최신 코드 가져오기
git pull origin main

# 서버 재시작
sudo systemctl restart workfair
# 또는
pm2 restart workfair
```


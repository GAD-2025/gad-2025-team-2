# 공고 관리 페이지 에러 디버깅 가이드

## 🔍 문제 진단

"공고를 불러오는데 실패했습니다" 에러가 발생하는 경우:

### 1. 브라우저 콘솔 확인

브라우저 개발자 도구(F12) → Console 탭에서 다음을 확인하세요:

1. **에러 메시지 확인:**
   - `공고 로딩 오류:` 메시지 확인
   - `에러 상세:` 메시지 확인
   - 실제 HTTP 상태 코드 확인

2. **Network 탭 확인:**
   - `/jobs` 요청이 있는지 확인
   - 요청 상태 코드 확인 (200, 404, 500 등)
   - 응답 본문 확인

### 2. 가능한 원인

#### 원인 1: user_id가 없음
- `localStorage.getItem('signup_user_id')`가 `null`
- 해결: 로그인 후 다시 시도

#### 원인 2: 백엔드 서버가 실행되지 않음
- `http://localhost:8000` 접속 불가
- 해결: 백엔드 서버 시작

#### 원인 3: 매장이 없음
- 고용주에게 등록된 매장이 없음
- 해결: 마이페이지에서 매장 추가

#### 원인 4: 타임아웃
- 서버 응답이 너무 느림
- 해결: 타임아웃을 10초로 증가 (이미 완료)

## ✅ 수정 완료 사항

1. ✅ 타임아웃 2초 → 10초로 증가
2. ✅ 에러 처리 개선 (빈 결과는 에러가 아님)
3. ✅ `user_id`가 `null`일 때 처리 개선
4. ✅ 백엔드에서 `user_id`가 빈 문자열일 때 처리 개선

## 🧪 테스트 방법

1. **브라우저 콘솔 열기** (F12)
2. **공고 관리 페이지 접속**
3. **Console 탭에서 확인:**
   ```
   Loaded X stores: [...]
   Loaded X jobs for employer
   ```
4. **에러가 발생하면:**
   - `공고 로딩 오류:` 메시지 확인
   - `에러 상세:` 메시지 확인
   - Network 탭에서 `/jobs` 요청 확인

## 📋 체크리스트

- [ ] 백엔드 서버 실행 중인가? (http://localhost:8000/docs)
- [ ] `localStorage.getItem('signup_user_id')` 값이 있는가?
- [ ] 매장이 등록되어 있는가? (마이페이지 확인)
- [ ] 브라우저 콘솔에 에러 메시지가 있는가?

## 🔧 추가 디버깅

브라우저 콘솔에서 다음 명령어 실행:

```javascript
// user_id 확인
console.log('user_id:', localStorage.getItem('signup_user_id'));

// API 테스트
fetch('http://localhost:8000/jobs?limit=10&user_id=' + localStorage.getItem('signup_user_id'))
  .then(r => r.json())
  .then(data => console.log('API 응답:', data))
  .catch(err => console.error('API 에러:', err));
```


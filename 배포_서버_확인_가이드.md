# 배포 서버 확인 가이드

## 🔗 배포 서버 링크

### 백엔드 서버
- **API 서버**: https://route.nois.club:3002
- **API 문서 (Swagger)**: https://route.nois.club:3002/docs
- **API 문서 (ReDoc)**: https://route.nois.club:3002/redoc
- **Health Check**: https://route.nois.club:3002/health

### 프론트엔드 서버
- **프론트엔드 URL**: (확인 필요 - 교수님/서버 관리자에게 문의)

---

## ✅ 확인 방법

### 1. GitHub Push 확인

```powershell
git log --oneline -3
git status
```

최신 커밋이 `origin/main`과 동기화되어 있는지 확인

---

### 2. 배포 서버 API 확인

#### 방법 1: 브라우저에서 확인

1. **API 문서 열기:**
   - https://route.nois.club:3002/docs

2. **확인할 엔드포인트:**
   - `/employer/stores/{user_id}` - 매장 목록 가져오기
   - `/api/posts` - Posts API
   - `/employer/profile/{user_id}` - 고용주 프로필

3. **엔드포인트 테스트:**
   - 각 엔드포인트를 클릭하여 "Try it out" 버튼 클릭
   - 파라미터 입력 후 "Execute" 클릭
   - 응답 확인

#### 방법 2: PowerShell에서 확인

```powershell
# 기본 연결 확인
curl https://route.nois.club:3002/

# Posts API 확인
curl https://route.nois.club:3002/api/posts

# 매장 목록 확인 (user_id는 실제 사용자 ID로 변경)
curl https://route.nois.club:3002/employer/stores/employer-test-001
```

---

### 3. 프론트엔드 확인

1. **프론트엔드 URL 접속** (확인 필요)
2. **마이페이지 접속**
3. **온보딩 정보 확인:**
   - 회사 이름 표시되는지
   - 주소 표시되는지
   - "기본가게" 태그 표시되는지

---

## 🔍 체크리스트

### GitHub Push 확인
- [ ] 최신 커밋이 `origin/main`에 push되었는지
- [ ] 변경사항이 모두 커밋되었는지

### 배포 서버 확인
- [ ] API 서버가 작동하는지 (https://route.nois.club:3002/docs)
- [ ] `/employer/stores/{user_id}` 엔드포인트가 있는지
- [ ] `/api/posts` 엔드포인트가 있는지
- [ ] 엔드포인트가 정상 작동하는지

### 프론트엔드 확인
- [ ] 프론트엔드 URL 확인
- [ ] 마이페이지에서 매장 정보 표시되는지
- [ ] "기본가게" 태그 표시되는지

---

## 🆘 문제 해결

### API 엔드포인트가 없는 경우

1. **배포 서버에 코드가 반영되지 않았을 수 있음**
   - 자동 배포: 5-10분 대기 후 다시 확인
   - 수동 배포: 서버 관리자에게 요청

2. **서버 재시작 필요할 수 있음**
   - 배포 서버 관리자에게 재시작 요청

### 프론트엔드가 업데이트되지 않은 경우

1. **프론트엔드도 push 필요할 수 있음**
   ```powershell
   git add frontend/
   git commit -m "프론트엔드 업데이트"
   git push origin main
   ```

2. **프론트엔드 빌드 필요할 수 있음**
   - 배포 서버에서 프론트엔드 빌드 및 배포 필요

---

## 📋 빠른 확인 명령어

```powershell
# GitHub 상태 확인
cd "C:\Users\peach\Downloads\gad-2025-team-2-main\gad-2025-team-2-main"
git log --oneline -3
git status

# 배포 서버 확인
curl https://route.nois.club:3002/
curl https://route.nois.club:3002/docs
curl https://route.nois.club:3002/api/posts
```





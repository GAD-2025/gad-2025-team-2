# SSH 접속이 안 될 때 대안 배포 방법

## 🔍 현재 상황

SSH 접속 시 "Permission denied" 오류 발생
- 비밀번호가 틀렸을 수 있음
- 또는 다른 사용자 이름일 수 있음

---

## ✅ 해결 방법

### 방법 1: 교수님/서버 관리자에게 확인

SSH 접속 정보 확인 요청:
- 정확한 사용자 이름 (`root`? `ubuntu`? `admin`?)
- 정확한 비밀번호
- SSH 키 사용 가능 여부

---

### 방법 2: GitHub Push 후 자동 배포 확인 (권장!)

SSH 접속이 안 되더라도, **GitHub에 push하면 자동 배포**가 설정되어 있을 수 있습니다.

#### 단계:

1. **Cursor에서 코드 수정**

2. **Git commit & push:**
   ```powershell
   cd "C:\Users\peach\Downloads\gad-2025-team-2-main\gad-2025-team-2-main"
   git add .
   git commit -m "변경사항 설명"
   git push origin main
   ```

3. **배포 서버 확인:**
   - 브라우저에서: https://route.nois.club:3002/docs
   - 5-10분 후 다시 확인
   - `/api/posts` 엔드포인트 확인
   - `/employer/stores/{user_id}` 엔드포인트 확인

4. **자동 배포가 설정되어 있다면:**
   - GitHub에 push하면 자동으로 배포 서버에 반영됨
   - SSH 접속 없이도 배포 가능!

---

### 방법 3: 다른 사용자 이름 시도

```powershell
# ubuntu 사용자 시도
ssh ubuntu@route.nois.club

# admin 사용자 시도
ssh admin@route.nois.club

# deploy 사용자 시도
ssh deploy@route.nois.club
```

각각 다른 비밀번호를 요청할 수 있습니다.

---

## 🚀 권장 워크플로우

### 현재 상황에서:

1. **Cursor에서 코드 수정**
2. **Git commit & push:**
   ```powershell
   git add .
   git commit -m "변경사항"
   git push origin main
   ```
3. **배포 서버 확인:**
   - https://route.nois.club:3002/docs
   - 5-10분 후 변경사항 확인

4. **자동 배포가 안 되면:**
   - 교수님/서버 관리자에게 배포 요청
   - 또는 SSH 접속 정보 확인 후 재시도

---

## ✅ 확인 방법

배포 후 확인:

```powershell
# PowerShell에서
curl https://route.nois.club:3002/docs
curl https://route.nois.club:3002/api/posts
```

브라우저에서:
- https://route.nois.club:3002/docs
- `/api/posts` 엔드포인트 확인
- `/employer/stores/{user_id}` 엔드포인트 확인

